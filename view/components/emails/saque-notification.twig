<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Novo Pedido de Saque - {{ NAME }}</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #333;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 600px;
            margin: 20px auto;
            background-color: #ffffff;
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(0,0,0,0.1);
            overflow: hidden;
        }
        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 30px;
            text-align: center;
        }
        .header h1 {
            margin: 0;
            font-size: 28px;
            font-weight: 300;
        }
        .content {
            padding: 40px 30px;
        }
        .alert-box {
            background-color: #fff3cd;
            border: 1px solid #ffeaa7;
            border-radius: 8px;
            padding: 20px;
            margin-bottom: 30px;
        }
        .alert-box h2 {
            color: #856404;
            margin-top: 0;
            font-size: 20px;
        }
        .info-section {
            background-color: #f8f9fa;
            border-radius: 8px;
            padding: 25px;
            margin-bottom: 25px;
        }
        .info-section h3 {
            color: #495057;
            margin-top: 0;
            font-size: 18px;
            border-bottom: 2px solid #dee2e6;
            padding-bottom: 10px;
        }
        .info-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 15px;
            padding: 10px 0;
            border-bottom: 1px solid #e9ecef;
        }
        .info-row:last-child {
            border-bottom: none;
            margin-bottom: 0;
        }
        .info-label {
            font-weight: 600;
            color: #495057;
            min-width: 120px;
        }
        .info-value {
            color: #6c757d;
            text-align: right;
            flex: 1;
        }
        .highlight {
            background-color: #e3f2fd;
            border-left: 4px solid #2196f3;
            padding: 15px;
            margin: 20px 0;
        }
        .highlight h4 {
            margin-top: 0;
            color: #1976d2;
        }
        .cta-button {
            display: inline-block;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 15px 30px;
            text-decoration: none;
            border-radius: 25px;
            font-weight: 600;
            margin-top: 20px;
            transition: transform 0.2s ease;
        }
        .cta-button:hover {
            transform: translateY(-2px);
        }
        .footer {
            background-color: #f8f9fa;
            padding: 20px 30px;
            text-align: center;
            color: #6c757d;
            font-size: 14px;
        }
        .status-pending {
            background-color: #fff3cd;
            color: #856404;
            padding: 8px 16px;
            border-radius: 20px;
            font-size: 14px;
            font-weight: 600;
            display: inline-block;
        }
        .amount {
            font-size: 24px;
            font-weight: 700;
            color: #28a745;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>💰 Novo Pedido de Saque</h1>
            <p>Um novo pedido de saque foi solicitado e requer sua atenção</p>
        </div>
        
        <div class="content">
            <div class="alert-box">
                <h2>⚠️ Ação Necessária</h2>
                <p>Um novo pedido de saque foi criado e está aguardando sua aprovação. Por favor, revise os detalhes abaixo e marque como concluído quando o pagamento for processado.</p>
            </div>

            <div class="info-section">
                <h3>🏨 Informações do Motel</h3>
                <div class="info-row">
                    <span class="info-label">Nome:</span>
                    <span class="info-value text-uppercase">{{ motel.nome|upper }}</span>
                </div>
                <div class="info-row">
                    <span class="info-label">ID:</span>
                    <span class="info-value">#{{ motel.id }}</span>
                </div>
                <div class="info-row">
                    <span class="info-label">Email:</span>
                    <span class="info-value">{{ motel.email }}</span>
                </div>
            </div>

            <div class="info-section">
                <h3>💳 Detalhes do Saque</h3>
                <div class="info-row">
                    <span class="info-label">Valor:</span>
                    <span class="info-value amount">R$ {{ saque.valor|number_format(2, ',', '.') }}</span>
                </div>
                <div class="info-row">
                    <span class="info-label">Status:</span>
                    <span class="info-value">
                        <span class="status-pending">{{ saque.status }}</span>
                    </span>
                </div>
                <div class="info-row">
                    <span class="info-label">Data:</span>
                    <span class="info-value">{{ saque.date_create|date('d/m/Y H:i') }}</span>
                </div>
                <div class="info-row">
                    <span class="info-label">ID do Saque:</span>
                    <span class="info-value">#{{ saque.id }}</span>
                </div>
            </div>

            <div class="info-section">
                <h3>🏦 Conta Bancária</h3>
                <div class="info-row">
                    <span class="info-label">Banco:</span>
                    <span class="info-value">{{ conta.banco_pix }}</span>
                </div>
                <div class="info-row">
                    <span class="info-label">Tipo de Conta:</span>
                    <span class="info-value">{{ conta.conta_tipo }}</span>
                </div>
                <div class="info-row">
                    <span class="info-label">Agência:</span>
                    <span class="info-value">{{ conta.conta_ag }}</span>
                </div>
                <div class="info-row">
                    <span class="info-label">Conta:</span>
                    <span class="info-value">{{ conta.conta_numero }}</span>
                </div>
                <div class="info-row">
                    <span class="info-label">Titular:</span>
                    <span class="info-value">{{ conta.conta_responsavel }}</span>
                </div>
                <div class="info-row">
                    <span class="info-label">CPF/CNPJ:</span>
                    <span class="info-value">{{ conta.conta_cpf_cnpj }}</span>
                </div>
            </div>

            <div class="highlight">
                <h4>📋 Próximos Passos</h4>
                <ol>
                    <li>Verifique se todas as informações estão corretas</li>
                    <li>Processe o pagamento para a conta bancária informada</li>
                    <li>Confirme o recebimento com o motel</li>
                    <li>Marque o saque como "Concluído" no painel administrativo</li>
                </ol>
            </div>

            <div style="text-align: center;">
                <a href="{{ DOMAIN }}/admin/saques" class="cta-button">
                    🔍 Acessar Painel de Saques
                </a>
            </div>
        </div>

        <div class="footer">
            <p><strong>{{ NAME }}</strong> - Sistema de Gestão de Motéis</p>
            <p>Este é um email automático. Não responda a esta mensagem.</p>
            <p>Data de envio: {{ "now"|date('d/m/Y H:i:s') }}</p>
        </div>
    </div>
</body>
</html>
